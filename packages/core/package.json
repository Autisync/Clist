{
  "name": "@fieldready/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod validation + types — the same schemas validate API input and (later) run client-side on the phone before a sync attempt. Architecture §2.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p ."
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
